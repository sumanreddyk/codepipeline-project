version: 0.2

proxy:
  logs: yes

env:
  variables:
    AWS_DEFAULT_REGION: ap-southeast-2


phases:
  install:
    commands:
      - npm -v
      - npm install yarn -g
      - yarn -v
      - npm install -g aws-cdk
      - cdk --help

  build:
    commands:
      - cd flask-docker-app
